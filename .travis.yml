language: python
python:
  - "2.7"
# command to install dependencies
install: "pip install -r REQUIREMENTS.txt"
# command to run tests
script: nose2 -t ..

before_install:
  - pip install codecov
after_success:
  - codecov
